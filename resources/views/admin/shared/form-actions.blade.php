<div class="admin-sticky-actions">
    <div class="admin-sticky-actions__inner">
        <button type="submit" class="btn btn-primary">{{ $saveLabel ?? __('catalog.actions.save') }}</button>
        <a href="{{ $cancelUrl }}" class="btn bg-secondary-subtle text-secondary">{{ $cancelLabel ?? __('catalog.actions.cancel') }}</a>
    </div>
</div>
